Weather API Project
This project is a simple Python application that fetches live weather data using a public API and displays it in the terminal.
Features
- Fetch live weather data by city name
- Parse JSON API response
- Display temperature, condition, and humidity
Requirements
- Python 3.x
- requests library
To install requests:
```bash
pip install requests
